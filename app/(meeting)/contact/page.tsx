export default function Page() {

 // if user -> whatspp -> to whatsapp
 // if user -> discord -> to discord 
 // if user -> gmail -> to gmail

 // if user -> Book a meeting -> this page -> to book a meeting (by default)

    return <h1>Hello, Home page!</h1>
  }